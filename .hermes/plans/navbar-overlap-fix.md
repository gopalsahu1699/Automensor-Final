# Task: Fix missing page modules + navbar overlapping content

## Issue 1: Missing page modules (TypeScript errors)
`.next/types/validator.ts` references 3 deleted page modules:
- `app/get-started/page.js` (should be `page.tsx`)
- `app/service-areas/page.js` (should be `page.tsx`)  
- `app/starter-package/page.js` (should be `page.tsx`)

These are auto-generated by Next.js. The fix is to delete the `.next/types/` folder so Next.js regenerates it on next build. Run:
```
rm -rf .next/types
```

## Issue 2: Navbar overlapping content on many pages
The fixed navbar (`glass-nav`) is ~80px tall (py-5 + logo height). Many pages start content at `py-16` or `pt-24` which is NOT enough — the navbar covers the top of the content.

### Pages that need fixing:

**app/faq/page.jsx** (line 68):
- Current: `className="bg-[#f5f6f7] py-16 px-4"`
- Fix: Change to `className="bg-[#f5f6f7] pt-28 pb-16 px-4"` (pt-28 = 112px, enough for navbar)

**app/privacy-policy/page.tsx** (line 116):
- Current: `<main className="bg-white min-h-screen">`
- Fix: Add `pt-28` → `<main className="bg-white min-h-screen pt-28">`

**app/terms-of-service/page.tsx** (line 161):
- Current: `<main className="bg-white min-h-screen">`
- Fix: Add `pt-28` → `<main className="bg-white min-h-screen pt-28">`

**components/home/HeroSection.jsx** (line 15):
- Current: `className="relative min-h-screen flex items-center pt-24 overflow-hidden"`
- Fix: Change `pt-24` to `pt-28` → `className="relative min-h-screen flex items-center pt-28 overflow-hidden"`

**components/solutions/AllSolutionsClient.tsx** (line 68):
- Current: `className="relative pt-40 pb-20 hero-mesh overflow-hidden"`
- This one has pt-40 which is enough, but verify it looks good

**app/all-solutions/page.tsx** (line 25):
- Current: `<div className="pt-20">`
- Fix: Change to `pt-28` → `<div className="pt-28">`

**components/Clients/ContactClient.tsx** (line 40):
- Current: `<div className="min-h-screen bg-background pt-20">`
- Fix: Change to `pt-28` → `<div className="min-h-screen bg-background pt-28">`

### General rule:
Every page's first visible section needs `pt-28` (112px) or more top padding to clear the fixed navbar. Check ALL pages and fix any that have less.

After all fixes:
1. Run `npx tsc --noEmit` — should show zero errors
2. Do NOT git push
